# LaravueDemo

### Laravel 配置

```bash
cd server
mv .env.example .env
composer install
php artisan migrate
php artisan db:seed
php artisan serve
```

### Vue 配置

```bash
cd frontend
npm install
gulp dev
```

